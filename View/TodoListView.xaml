<UserControl x:Class="TodoList.View.TodoListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TodoList.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Label
           Grid.Row="0"           
           Content="TodoList"
           HorizontalAlignment="Center"           
           HorizontalContentAlignment="Center"
           FontSize="30"
           Height="42"
           VerticalAlignment="Top"
           FontWeight="Bold"/>
        <TextBox
            Grid.Row="1"            
            Height="25"
            FontSize="15"
            x:Name="SearchBox"
            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
            VerticalAlignment="Top"/>
        <TextBlock
           Grid.Row="1"           
           Margin="5,0,0,0"
           Height="25"
           FontSize="15"                   
           Text="Serch Keyword" 
           Foreground="#A0A0A0"
           IsHitTestVisible="False"
           VerticalAlignment="Top">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <Grid
            Grid.Row="2"
            Grid.RowSpan="2">
            <ListBox             
                Margin="0,-20,0,0"
                ItemsSource="{Binding TodoItems}"            
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                FontSize="18"             
                BorderThickness="0"
                VerticalAlignment="Top">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#CCCCCC" 
                                BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>                                   
                                    <ColumnDefinition Width="5*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    FontSize="18"                             
                                    Text="{Binding TITLE}"/>            
                                <DockPanel
                                    Grid.Column="1"
                                    Margin="5"
                                    LastChildFill="False">
                                    <Button
                                        Style="{StaticResource TodoListButton}"
                                        Content="削除"
                                        Command="{Binding DataContext.DeliteCommand,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>                                    
                                    <Button
                                        Style="{StaticResource TodoListButton}"
                                        Content="更新"                                         
                                        Command="{Binding DataContext.ShowEditCommand,RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                                    <Button
                                        Style="{StaticResource TodoListButton}"
                                        Content="詳細"                                        
                                        Command="{Binding DataContext.ShowDetailCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>                   
                                </DockPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
